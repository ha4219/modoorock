import React from 'react';
import {StyleSheet, ImageBackground, View} from 'react-native';

const MissionImage = ({uri}) => {
  return <ImageBackground />;
};

export default MissionImage;

const styles = StyleSheet.create({});
